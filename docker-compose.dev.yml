services:
  embedding:
    build:
      context: ./embedding

  api:
    build:
      context: ./backend

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE: ${VITE_API_BASE:-http://localhost:8000}
